# rp-tooling

Streaming triage: Wikipedia recent-changes → Redpanda Connect → LLM reasoning service → Postgres → web view. All local, one `docker compose up`.

(Build in progress — run instructions land with the first working pipeline.)
